import React from 'react';
import { Route, Routes } from 'react-router-dom';

const BoardMain = () => {
    return (
        <Routes>
            <Route />
        </Routes>
    );
};

export default BoardMain;